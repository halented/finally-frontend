import React, { Component } from 'react';

class Metrics extends Component {
    render(){
        return (
            <button>
                MATRACSK
            </button>
        )
    }
}

export default Metrics